version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - whoami

  whoami:
    image: traefik/whoami
    deploy:
      replicas: 5

  apachebench:
    image: httpd:latest
